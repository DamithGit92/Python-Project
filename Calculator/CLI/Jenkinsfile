pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout([$class: 'GitSCM', branches: [[name: '*/master']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[credentialsId: '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', url: 'https://github.com/DamithGit92/Python-Project.git']]])
            }
        }
        stage('Build') {
            steps {
                sh 'pip install -r Dockerfile'
                sh 'python Calculator/CLI/CliCalculator.py build'
            }
        }
    }
}

